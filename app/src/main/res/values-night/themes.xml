<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FinalExam" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--This is the parent style view used for textview-->
    <style name="customTextView" parent="Widget.AppCompat.TextView">
        <item name="fontFamily">sans-serif-black</item>
        <item name="android:textSize">90sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#14cba8</item>
    </style>

    <!--Style for the first text view-->
    <style name="FirstTextView" parent="customTextView">
        <item name="android:textAlignment">center</item>
    </style>

    <!--Style for the second text view-->
    <!--
            This was created because the first textview has a text
            alignment of center. The second text view does not need
            the alignment because it will be displayed along the first
            text view
    -->
    <style name="SecondTextView" parent="customTextView">
    </style>

    <!--Style for all the buttons-->
    <style name="Button" parent="Widget.AppCompat.Button">
        <item name="backgroundTint">@color/primaryDarkColor</item>
    </style>
</resources>